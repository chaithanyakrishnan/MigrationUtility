# MigrateIQ Backend — Python 3.9+
# Install: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0

# ── AI / LLM ──────────────────────────────────────────────────
anthropic>=0.30.0

# ── Database ──────────────────────────────────────────────────
# sqlalchemy[asyncio] pulls greenlet automatically (required for async engine)
sqlalchemy[asyncio]>=2.0.30
alembic>=1.13.0
asyncpg>=0.29.0          # async driver used by SQLAlchemy at runtime
psycopg2-binary>=2.9.9   # sync driver used by Alembic migrations only
greenlet>=3.0.0           # explicit pin — SQLAlchemy async hard dependency
pgvector>=0.3.0
redis>=5.0.0

# ── Data processing ───────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
networkx>=3.0.0
pyyaml>=6.0.0
jinja2>=3.1.0

# ── File parsing ──────────────────────────────────────────────
pypdf>=3.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
# OCR for screenshots embedded in Word docs (FRP schema uploads).
# Requires the tesseract binary on the host: `brew install tesseract`.
pytesseract>=0.3.10
Pillow>=10.0.0

# ── Validation / serialisation ────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-jose[cryptography]>=3.3.0

# ── Observability ─────────────────────────────────────────────
structlog>=24.0.0
prometheus-client>=0.20.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── OCR (schema screenshots embedded in DOCX/PDF) ─────────────
# Requires the tesseract binary on the host: `brew install tesseract`
pytesseract>=0.3.10
Pillow>=10.0.0
pymupdf>=1.24.0   # render image-only PDF pages to images for OCR

# ── Testing ───────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
